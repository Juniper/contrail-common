package db

var tableDefs = []string{
{% for schema in schemas %}{% if schema.Type != "abstract" and schema.ID %}
"create table {{schema.ID }} ({% for column, type in schema.SQLTypes %}`{{column}}` {{type}}{% if not forloop.Last %},{% endif %}{% endfor %});",
{% endif %}
{% endfor %}
}