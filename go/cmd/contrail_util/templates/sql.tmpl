package db

var tableDefs = []string{
{% for schema in schemas %}{% if schema.Type != "abstract" and schema.ID %}
"create table {{schema.ID }} ({% for column in schema.Columns %}`{{column.Column}}` {{column.Type}}{% if not forloop.Last %},{% endif %}{% endfor %});",
{% endif %}
{% endfor %}
}